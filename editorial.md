把不可通行的位置视为墙。原网格左上角与右下角不连通，等价于这些墙形成一道从“左边界或下边界”到“上边界或右边界”的连续隔断。

这正好是平面图的割与对偶路径的对应。于是，原问题可以先转化为对偶图中的路径问题。

### 建立对偶图并收缩已有墙

每个内部网格交点对应一个顶点，共 $(N-1)^2$ 个。再将左边界和下边界合并为顶点 $X$，上边界和右边界合并为顶点 $Y$。

每一对相邻单元格之间的分界线，对应一条沿着这条分界线、连接其两个端点的对偶边。它横穿的是两个单元格中心之间的原图边。

具体地，将内部交点记为 $(i,j)$，其中 $1\le i,j<N$。

* 上下相邻单元格 $(i,j),(i+1,j)$ 的分界线，连接内部交点 $(i,j-1),(i,j)$。左端越界时替换为 $X$，右端越界时替换为 $Y$。
* 左右相邻单元格 $(i,j),(i,j+1)$ 的分界线，连接内部交点 $(i-1,j),(i,j)$。上端越界时替换为 $Y$，下端越界时替换为 $X$。

五种状态分别这样处理。

* 已有墙始终属于隔断，可以免费经过，使用并查集收缩其对偶边。
* 普通门只有被封锁时才属于隔断，对应一条可以选择的边。
* 两扇特殊门分别对应两条指定边，仍记为 $A,B$。
* 无阻隔处无法封锁，不加入对偶图。

收缩后，如果 $X,Y$ 已经成为同一个点，说明原网格最初就不连通，而封门不可能使它重新连通，直接输出 $-1$。

### 转化为经过两条指定边的最短简单路径

设选中封锁的普通门集合为 $F$。在收缩后的对偶图中，题目的条件等价于以下四条。

$$
\begin{aligned}
X&\not\leftrightsquigarrow Y &&\text{仅加入 }F\\
X&\not\leftrightsquigarrow Y &&\text{加入 }F\cup\{A\}\\
X&\not\leftrightsquigarrow Y &&\text{加入 }F\cup\{B\}\\
X&\leftrightsquigarrow Y &&\text{加入 }F\cup\{A,B\}
\end{aligned}
$$

若 $F$ 合法，在最后一个图中任取一条简单 $X$ 到 $Y$ 路径。它必须同时经过 $A,B$，否则只封锁其中一扇特殊门就已经能形成隔断。路径上的普通门不超过 $|F|$ 扇。

反过来，如果找到一条同时经过 $A,B$ 的简单 $X$ 到 $Y$ 路径，只封锁它上面的普通门即可。已有墙已经全部收缩，所选边在缩图中恰好形成一条简单路径；缺少 $A,B$ 中的任意一条都不能连通 $X,Y$，两条都加入后才连通。

所以问题严格等价于：求同时经过指定边 $A,B$ 的最短简单 $X$ 到 $Y$ 路径。若其长度为 $d$，答案就是 $d-2$，因为特殊门不计入普通门封锁数。

这里必须要求简单路径。允许随意绕路的话，可能走进一个分支经过特殊门后再返回，不能保证这扇门对于隔断是必要的。

### 处理自环和重边

收缩已有墙后，先将图化为简单无向图。

1. 普通门变成自环时直接删除，简单路径不会使用它。
2. 若 $A$ 或 $B$ 变成自环，则无解。
3. 同一对顶点之间有多扇普通门，只保留一条，选择其中任意一扇的代价都是 $1$。
4. 若普通门与某扇特殊门平行，只保留特殊门。目标简单路径既然必须经过这扇特殊门，就不可能再经过与它平行的普通门。
5. 若 $A,B$ 互相平行，则无解，因为简单路径不能同时使用它们。

按无序端点对汇总边的种类，就可以完成这些处理。

记此时的简单无向图为 $G$，顶点数、边数分别为 $V,E$，起点、终点分别为 $s,t$。接下来只讨论这张图。

### 对游走的权值求和

普通的状态搜索无法限制顶点不重复。考虑给每条无向边 $e$ 赋一个独立随机权值 $z(e)$，对所有游走的边权乘积求和，而不是直接判断游走是否存在。

所有运算在特征为 $2$ 的有限域 $\mathbb F_{2^{64}}$ 中进行。它的关键性质如下。

$$
x+x=0
$$

对于一条游走 $W$，定义权值为它每一步经过的边权之积，同一条边反复经过时也反复计入。

$$
\operatorname{weight}(W)=\prod_{e\text{ 按游走次序出现于 }W}z(e)
$$

只要求特殊边 $A,B$ 各自至多经过一次，普通边允许任意重复，包括走过去后立即返回。

设 $f(d,v,S)$ 为从 $s$ 出发、恰好走 $d$ 步、到达 $v$，且已经经过的特殊边集合恰为 $S$ 的所有游走的权值和。其中 $S\subseteq\{A,B\}$，只有四种状态。

初值为 $f(0,s,\varnothing)=1$，其余为 $0$。对于普通边 $e=\{u,v\}$，有以下转移。

$$
\begin{aligned}
f(d+1,v,S)&\gets f(d+1,v,S)+z(e)f(d,u,S)\\
f(d+1,u,S)&\gets f(d+1,u,S)+z(e)f(d,v,S)
\end{aligned}
$$

对于特殊边 $e=\{u,v\}$，仅当 $e\notin S$ 时允许转移，并将集合改为 $S\cup\{e\}$。

$$
\begin{aligned}
f(d+1,v,S\cup\{e\})&\gets f(d+1,v,S\cup\{e\})+z(e)f(d,u,S)\\
f(d+1,u,S\cup\{e\})&\gets f(d+1,u,S\cup\{e\})+z(e)f(d,v,S)
\end{aligned}
$$

逐层计算 $d=1,2,\dots,V-1$，第一次出现 $f(d,t,\{A,B\})\ne0$ 时输出 $d-2$。如果始终为零，则输出 $-1$。

下面证明这个判定为什么成立。特别地，不能简单声称“任意非简单游走与反向绕环的游走抵消”，因为 $u\to v\to u$ 反向后还是它本身。

### 只需证明最早非零的长度

先把各个 $z(e)$ 视为独立的不定元，而不是已经取好的随机数。设最短目标简单路径的长度为 $d^\ast$。

我们真正需要的是以下两个结论。

* 对所有 $d<d^\ast$，$f(d,t,\{A,B\})$ 是零多项式。
* 当 $d=d^\ast$ 时，$f(d,t,\{A,B\})$ 恰好等于全部最短目标简单路径的权值和，是非零多项式。

不需要证明所有长度的非简单游走都抵消。例如，已经存在一条目标简单路径后，在普通边上增加往返，确实可能对更大长度产生贡献。

下面给出两种彼此独立的证明。第一种直接构造游走之间的对合，第二种通过行列式展开，将游走的生成函数写成简单路径与匹配的求和。

### 证明方法一：构造无不动点对合

下面采用回文收缩与翻转思路，为需要消去的游走构造一个无不动点的对合，也就是满足以下性质的映射 $\varphi$。

$$
\varphi(W)\ne W,\qquad
\varphi(\varphi(W))=W
$$

同时要求两条游走经过每条无向边的次数相同。这样，它们的权值乘积相等，便能在特征 $2$ 下两两抵消。

### 先收缩回文闭合段

将一条游走写成顶点序列 $W=(v_0,v_1,\dots,v_d)$，其中 $v_0=s,v_d=t$，特殊边 $A,B$ 各自恰好经过一次。

若 $v_p=v_q$ 且 $p<q$，则区间 $W[p,q]=(v_p,\dots,v_q)$ 是一个闭合段。若它的顶点序列正反相同，则称它为回文闭合段。例如 $u,v,u$ 和 $u,v,w,v,u$ 都是回文闭合段。

**回文闭合段不可能包含特殊边。** 因为图中没有自环，回文闭合段的边数必须为偶数，否则正中间的两个相邻顶点相同。其前半段与后半段沿相反方向经过相同的边，所以每条无向边都出现偶数次。而 $A,B$ 在整条游走中各自只出现一次，因此都不能位于这样的闭合段中。

故可以在辅助过程中将一个回文闭合段收缩为它的端点，既不改变游走的起终点，也不会删掉特殊边。

不过，收缩会改变长度与权值，不能直接作为最终的配对操作。它只用于确定最终应该翻转哪一个闭合段，真正配对时仍然保留原游走的全部边。

### 确定唯一的翻转区间

从 $W$ 的一个临时副本出发，并为每个顶点的每次出现保留其在原游走中的位置。反复执行以下过程。

1. 在当前临时游走中，找一个出现至少两次的顶点 $u$，要求它的第一次出现位置最靠前。
2. 取从 $u$ 的第一次出现到最后一次出现之间的整个闭合段。
3. 若这一段是回文，就将其收缩为一个 $u$，保留左端那次出现，继续寻找。
4. 若这一段不是回文，则停止，记录这两个端点在原游走中的位置 $p,q$。

这里必须取当前顶点的**第一次到最后一次**，而不是任意两次出现。收缩后，$u$ 在临时游走中只剩一次；在它之前的顶点原先就都只出现一次，收缩也不会增加它们的出现次数。因此，下一个被选择的重复顶点一定在这次收缩位置的右侧。

进一步地，若先后收缩了 $k\ge1$ 个区间，它们在原游走中为 $[p_1,q_1],\dots,[p_k,q_k]$，最终选出的非回文区间为 $[p,q]$，则有以下关系。

$$
p_1<q_1<p_2<q_2<\cdots<p_k<q_k<p<q
$$

也就是说，所有收缩区间都互不相交，并且依次从左向右处理。每次收缩只改变已经处理的前部，尚未处理的后缀始终与原游走完全相同。因此，最后找到的非回文闭合段，在原游走中就是未经改动的 $W[p,q]$。若一开始就遇到非回文段，没有进行任何收缩，这个结论也显然成立。

**记录的是具体出现位置，不能只记录顶点名称。** 最终选中的顶点可能曾经出现在已经收缩掉的段内，所以不能回到原游走后，再重新取这个顶点的全局第一次出现作为左端点。这里的 $p,q$ 始终指临时游走选中的两次出现所对应的原位置。

### 为什么一定能找到非回文段

先考虑 $d\le d^\ast$ 的非简单游走。

假设上述过程始终只收缩回文段，最后没有重复顶点了。由于收缩不改变起终点，也不删除 $A,B$，最终得到的就是一条仍经过 $A,B$ 的简单 $s$ 到 $t$ 路径。

原游走不是简单路径，因此至少进行过一次收缩，每次收缩又都严格减少长度。于是得到一条长度严格小于 $d\le d^\ast$ 的目标简单路径，与 $d^\ast$ 的最小性矛盾。

每次收缩都会减少长度，过程不可能无限进行，因此一定会停在某个非回文闭合段。

若根本不存在目标简单路径，同样不可能通过收缩得到目标简单路径，所以任意长度的这类游走都一定能够找到非回文闭合段。

### 回到原游走进行翻转

对选出的原位置 $p,q$，只将 $W[p,q]$ 反向，其余部分不动，得到以下映射。

$$
\varphi(W)=
(v_0,\dots,v_{p-1},v_q,v_{q-1},\dots,v_p,v_{q+1},\dots,v_d)
$$

若 $p=0$ 或 $q=d$，对应的区间外前缀或后缀视为空。

因为 $v_p=v_q$，翻转前后能够与区间外的部分正确衔接；图是无向图，反向经过的边仍然存在。所以 $\varphi(W)$ 仍然是同起点、同终点、同长度的合法游走。

翻转不改变任何无向边的使用次数，因此特殊边仍各自恰好经过一次，普通边的使用次数也不变。选中的闭合段仍然使端点重复出现，所以配对后的游走仍然不是简单路径。

由于 $W[p,q]$ 不是回文，反向后顶点序列不同，因此映射没有不动点。

$$
\varphi(W)\ne W
$$

### 为什么再做一次会选中同一区间

令 $W'=\varphi(W)$。需要证明，对 $W'$ 再执行辅助过程，仍会收缩同样的回文段，并最终选出同一对位置 $p,q$。

按收缩次数归纳。假设之前的收缩在两条游走中完全相同，考虑 $W$ 接下来选择的某个回文段，其端点顶点为 $u$。

这段回文位于最终翻转区间 $[p,q]$ 之前，所以在 $W'$ 中原封不动。还需检查，算法会不会因为后面的翻转而改选其他位置。

* 在 $u$ 第一次出现之前的所有顶点，当前都只出现一次。翻转只是重新排列后面的一段，不改变每个顶点的出现次数，也不改变这些前部位置，所以它们仍然不会成为重复顶点。
* 这个回文段结束于 $u$ 的最后一次出现，因此 $u$ 根本不出现在其后的后缀中。翻转后面的区间不会改变 $u$ 的第一次、最后一次出现，也不会改变它们之间的回文段。

因此，$W'$ 同样选中 $u$，并进行完全相同的收缩。由归纳可知，所有此前的回文收缩都会被逐个重现。

最后到达原位置 $p$ 时，其前面的剩余顶点都只出现一次，这一点在翻转后仍然成立。设 $v_p=v_q=u$，当前 $p,q$ 分别是 $u$ 的第一次和最后一次出现；翻转闭合段后，两端仍然都是 $u$，外部也没有新增的 $u$，所以再次选择的仍是这两个端点。

这一次看到的闭合段恰好是原非回文段的反向。非回文串的反向仍然非回文，因此辅助过程仍在这里停止。再次翻转相同区间就恢复原游走，得到以下关系。

$$
\varphi(\varphi(W))=W
$$

至此，$\varphi$ 确实是所需的无不动点对合。

### 两两抵消后只剩最短简单路径

翻转只改变边的顺序和方向，不改变每条无向边的使用次数，所以有以下等式。

$$
\operatorname{weight}(\varphi(W))=\operatorname{weight}(W)
$$

特征为 $2$，每一对的贡献因此为 $0$。

$$
\operatorname{weight}(W)+\operatorname{weight}(\varphi(W))=0
$$

当 $d<d^\ast$ 时，所有被统计的游走都不是简单路径，且都可以按上述对合配对，因此整个多项式恒为零。

当 $d=d^\ast$ 时，所有非简单游走仍然可以配对消去，剩下的恰好是全部最短目标简单路径。记它们的集合为 $\mathcal P^\ast$，便得到以下结论。

$$
\begin{aligned}
f(d,t,\{A,B\})&=0 &&(d<d^\ast)\\
f(d^\ast,t,\{A,B\})
&=
\sum_{P\in\mathcal P^\ast}
\prod_{e\in P}z(e)
\end{aligned}
$$

若根本不存在目标简单路径，任意长度的游走都可以配对，所有长度的结果均恒为 $0$。

注意，辅助收缩完全不需要实际执行，它只用于证明配对存在。DP 仍然统计全部允许的游走，不能额外禁止普通边立即往返，否则翻转后的游走可能离开统计范围，无法直接使用这套对合证明。

### 证明方法二：行列式与路径展开

另一种方法是不直接给游走配对，而是将所有游走写成矩阵逆的形式幂级数，再利用行列式展开消去多余的循环。以下矩阵、行列式和形式标记都只用于证明，不需要在实际算法中计算。

记 $\operatorname{V}(G),\operatorname{E}(G)$ 分别为图的顶点集合和边集合，顶点已经编号为 $1,\dots,V$。已有预处理保证 $s\ne t$，且图中没有自环和重边。

### 用矩阵幂记录受限游走

仍将每条边的权值 $z(e)$ 视为独立不定元。引入两个形式标记 $\alpha,\beta$，分别标记经过特殊边 $A,B$，并在以下交换环中计算。

$$
\mathcal R=
\mathbb F_2\bigl[z(e)\ (e\in\operatorname{E}(G)),\alpha,\beta\bigr]
\big/\bigl(\alpha^2,\beta^2\bigr)
$$

也就是说，规定 $\alpha^2=\beta^2=0$，但保留 $\alpha\beta$。每个元素都可以唯一写成以下形式，其中四个系数只与边权不定元有关。

$$
a+b\alpha+c\beta+d\alpha\beta
$$

这些是附加的形式标记，不是在有限域中寻找平方为零的非零元素。截断的作用是：只要某扇特殊门经过两次，其权值乘积就变为零，恰好排除 DP 中不允许的游走。

定义标记边权如下。

$$
\widetilde z(e)=
\begin{cases}
\alpha z(e),&e=A\\
\beta z(e),&e=B\\
z(e),&e\notin\{A,B\}
\end{cases}
$$

令 $M$ 为对称加权邻接矩阵，用 $M(u,v)$ 表示其第 $u$ 行、第 $v$ 列的元素。

$$
M(u,v)=
\begin{cases}
\widetilde z(\{u,v\}),&\{u,v\}\in\operatorname{E}(G)\\
0,&\text{否则}
\end{cases}
$$

由矩阵乘法的定义，$M^d(s,t)$ 枚举所有从 $s$ 到 $t$、长度为 $d$ 的游走，并将每条游走的标记边权相乘后求和。于是有以下等式。

$$
[\alpha\beta]M^d(s,t)=f(d,t,\{A,B\})
$$

其中 $[\alpha\beta]$ 表示取 $\alpha\beta$ 的系数。

再引入记录长度的形式变量 $x$，令 $Q=I-xM$。在关于 $x$ 的形式幂级数中，有以下矩阵几何级数。

$$
Q^{-1}=(I-xM)^{-1}
=\sum_{d=0}^{\infty}x^dM^d
$$

这里不涉及数值收敛：将右侧乘上 $I-xM$，每个正次数的项都抵消，只剩单位矩阵。因此以下生成函数恰好是逆矩阵相应位置的系数。

$$
\sum_{d\ge0}f(d,t,\{A,B\})x^d
=[\alpha\beta]Q^{-1}(s,t)
$$

接下来分别分析逆矩阵表达式中的分母和分子。

### 分母为什么只剩普通边匹配

对任意顶点集合 $U$，记 $Q[U]$ 为同时保留 $U$ 对应行、列得到的主子矩阵。考虑它的行列式展开。

$$
\operatorname{det}(Q[U])
=
\sum_{\pi\in\operatorname{Sym}(U)}
\operatorname{sgn}(\pi)
\prod_{u\in U}Q(u,\pi(u))
$$

其中 $\operatorname{Sym}(U)$ 为 $U$ 上的所有排列。特征为 $2$，所以 $-1=1$，排列的正负号在这里相同。

将每个排列拆成互不相交的循环，并把 $u\mapsto\pi(u)$ 看成一条有向边。

* 不动点 $u\mapsto u$ 贡献 $Q(u,u)=1$，对应不选这个顶点。
* 二元循环 $u\mapsto v\mapsto u$ 贡献 $x^2\widetilde z(\{u,v\})^2$。如果这条边是特殊边，贡献为零；否则贡献为 $x^2z(\{u,v\})^2$。
* 长度至少为 $3$ 的循环可以反向。由于 $Q$ 对称，反向前后乘积相等，而排列不同，所以两项抵消。

若一个排列有多个长循环，就在所有长循环涉及的顶点中取编号最小的顶点，并反向它所在的循环。反向不改变循环涉及的顶点集合，再操作一次仍选中同一个循环，因此这是无不动点的配对。

消去所有长循环后，只剩互不相交的普通边二元循环，也就是普通边构成的匹配。于是定义以下多项式。

$$
D(U)=
\sum_{\substack{F\text{ 是 }G[U]\text{ 中的匹配}\\
F\cap\{A,B\}=\varnothing}}
x^{2|F|}\prod_{e\in F}z(e)^2
$$

有以下等式。

$$
\operatorname{det}(Q[U])=D(U)
$$

空匹配贡献 $1$，所以 $D(U)$ 的常数项为 $1$；其他项的 $x$ 次数都是正偶数。特别地，$D(\varnothing)=1$，且以下分母不含 $\alpha,\beta$。

$$
\operatorname{det}(Q)=D(\operatorname{V}(G))
$$

### 逆矩阵为什么对应交换下标的余子式

记 $\operatorname{minor}(Q,i,j)$ 为删去 $Q$ 的第 $i$ 行、第 $j$ 列后得到的矩阵，定义代数余子式如下。

$$
\operatorname{cof}(Q,i,j)
=(-1)^{i+j}\operatorname{det}(\operatorname{minor}(Q,i,j))
$$

伴随矩阵是代数余子式矩阵的转置。因此，逆矩阵的 $(s,t)$ 位置满足以下等式，注意右侧的行列下标是 $(t,s)$。

$$
Q^{-1}(s,t)
=\frac{\operatorname{cof}(Q,t,s)}{\operatorname{det}(Q)}
$$

这一关系在当前交换环中仍然成立。伴随矩阵恒等式只涉及加法与乘法，而 $\operatorname{det}(Q)$ 的常数项为 $1$，在形式幂级数中可逆，所以可以除以它。

也可以直接从删去第 $t$ 行、第 $s$ 列后的排列项，看出这两个下标的含义。把选中的矩阵元素 $Q(u,v)$ 看作有向边 $u\to v$，则每个展开项满足以下度数要求。

* $s$ 所在的行保留、列删除，所以 $s$ 的出度为 $1$、入度为 $0$。
* $t$ 所在的行删除、列保留，所以 $t$ 的出度为 $0$、入度为 $1$。
* 其余顶点对应的行、列均保留，所以入度和出度都为 $1$。

从 $s$ 沿出边前进，不可能走入已经经过的顶点。若第一次重复的顶点是 $s$，就违反 $s$ 入度为 $0$；若是其他顶点，它会同时接收链上的前驱和这次回到它的边，违反入度为 $1$。同理，这条链也不能汇入另一个循环。

因此，这条链一定在唯一没有出边的 $t$ 处结束，形成一条简单 $s$ 到 $t$ 路径。没有被这条路径使用的顶点则分解成若干循环，包括不动点。

这已经解释了“余子式中出现一条开放链”的原因。下面再用一条带标记的新增边，将这种解释写成严格的系数恒等式。

### 用新增有向边标记代数余子式

引入另一个独立标记 $y$。令 $\Delta$ 为仅在第 $t$ 行、第 $s$ 列等于 $1$、其余位置为 $0$ 的矩阵，并定义以下矩阵。

$$
\begin{aligned}
\Delta(u,v)&=[u=t][v=s]\\
\widehat Q&=Q+y\Delta
\end{aligned}
$$

也就是说，只把矩阵元素 $Q(t,s)$ 改成 $Q(t,s)+y$。这是临时加入一条权值为 $y$ 的**有向边 $t\to s$**，不同时加入带 $y$ 的反向边。

这里是加上一个新项，而不是覆盖原来的矩阵元素。即使图中已经存在无向边 $\{s,t\}$，原来的边权仍然保留；在展开中选择 $y$，才能表示使用了新增的标记边。

沿第 $t$ 行展开行列式。只有第 $s$ 列的元素改变，而且它的代数余子式删去了整行 $t$，不再含有被修改的元素。因此有以下恒等式。

$$
\operatorname{det}(\widehat Q)
=\operatorname{det}(Q)+y\operatorname{cof}(Q,t,s)
$$

特别地，$y$ 的次数至多为 $1$，并且有以下关系。

$$
[y]\operatorname{det}(\widehat Q)=\operatorname{cof}(Q,t,s)
$$

代入逆矩阵公式，就把目标分子转化为了一个完整行列式的一次项。

$$
Q^{-1}(s,t)
=
\frac{[y]\operatorname{det}(\widehat Q)}
{D(\operatorname{V}(G))}
$$

### 标记边所在的循环怎样变成路径

现在从排列展开的角度计算 $[y]\operatorname{det}(\widehat Q)$。要产生一个 $y$，必须满足 $\pi(t)=s$，并且在元素 $Q(t,s)+y$ 中选中 $y$ 这一项。

排列中，顶点 $t$ 所在的循环因此包含有向边 $t\to s$。从 $s$ 开始沿这个循环前进，可以唯一写成以下形式。

$$
s=u_0\to u_1\to\cdots\to u_r=t\to s
$$

最后一步是新增的标记边。排列的一个循环在闭合之前不会重复顶点，因此 $u_0,\dots,u_r$ 两两不同。删去最后的 $t\to s$，得到的恰好是一条简单 $s$ 到 $t$ 路径。

对这条路径上的每一步 $u_i\to u_{i+1}$，两个端点不同，必须选取 $Q$ 的非对角元素。由于特征为 $2$，有以下等式。

$$
Q(u_i,u_{i+1})
=-xM(u_i,u_{i+1})
=x\widetilde z(\{u_i,u_{i+1}\})
$$

所以，若记路径为 $P$、边数为 $|P|=r$，这个包含标记边的循环贡献如下。

$$
yx^{|P|}\prod_{e\in P}\widetilde z(e)
$$

新增边的权值是 $y$，不是 $xy$。因此，取 $y$ 的系数后留下的 $x$ 次数正好是原路径的边数，不会多算封闭循环的最后一步。

反过来，任意一条简单 $s$ 到 $t$ 路径，加上新增的 $t\to s$，都会唯一确定这样一个排列循环。因此，带标记的循环与简单 $s$ 到 $t$ 路径一一对应，不需要再乘循环长度，也不会因选择不同的循环起点而重复计数。

这一循环不能像普通长循环一样被反向消去，因为反向后新增标记边会变成 $s\to t$，而我们没有在那个矩阵位置加入 $y$。即使原图存在普通的 $s\to t$ 边，它也不带 $y$，不能代替标记边参与这次配对。

### 固定路径后为什么得到补集上的匹配

固定标记边所在的循环，也就固定了一条简单路径 $P$。记 $\operatorname{V}(P)$ 为路径上的顶点集合，这个循环已经为其中每个顶点选定了唯一的出边和入边。

排列中的其他循环不能再使用这些顶点：再次使用某一行或某一列，都会违反排列每行、每列恰好选择一个元素的要求。所以剩下的部分只能在以下顶点集合上形成一个独立排列。

$$
U=\operatorname{V}(G)\setminus\operatorname{V}(P)
$$

反过来，任取 $U$ 上的一个排列，与固定的标记循环放在一起，就得到完整顶点集合上的一个排列。两部分涉及的行、列互不相交，权值相乘，没有其他约束。

因此，固定路径 $P$ 后，所有剩余排列的贡献之和恰好为 $\operatorname{det}(Q[U])$。根据前面对普通行列式的分析，剩余长循环两两抵消，特殊边二元循环贡献为零，只有普通边匹配保留下来，其总贡献就是 $D(U)$。

记 $\mathcal Q$ 为所有简单 $s$ 到 $t$ 路径的集合，此时还没有要求它们经过特殊边。对所有路径求和，得到以下等式。

$$
[y]\operatorname{det}(\widehat Q)
=
\sum_{P\in\mathcal Q}
x^{|P|}
\left(\prod_{e\in P}\widetilde z(e)\right)
D\bigl(\operatorname{V}(G)\setminus\operatorname{V}(P)\bigr)
$$

### 提取两扇特殊门的系数

每条简单路径都不会重复边，因此它使用每扇特殊门的次数只能是 $0$ 或 $1$。于是标记边权的乘积可以写成以下形式。

$$
\prod_{e\in P}\widetilde z(e)
=
\alpha^{[A\in P]}\beta^{[B\in P]}
\prod_{e\in P}z(e)
$$

而路径外的匹配多项式 $D(U)$ 完全不含 $\alpha,\beta$。因此，一个求和项能够贡献 $\alpha\beta$，当且仅当路径本身同时包含 $A,B$。

这里不能把“一扇特殊门在路径上，另一扇在某个剩余循环中”的情况计入。剩余的特殊边二元循环含有 $\alpha^2$ 或 $\beta^2$，贡献为零；剩余长循环则已经通过反向配对消去。所以匹配部分不可能再提供任何特殊门标记。

令 $\mathcal P\subseteq\mathcal Q$ 为同时经过 $A,B$ 的简单 $s$ 到 $t$ 路径集合，取系数后得到以下分子。

$$
[\alpha\beta][y]\operatorname{det}(\widehat Q)
=
\sum_{P\in\mathcal P}
x^{|P|}
\left(\prod_{e\in P}z(e)\right)
D\bigl(\operatorname{V}(G)\setminus\operatorname{V}(P)\bigr)
$$

最后，分母 $D(\operatorname{V}(G))$ 的常数项为 $1$，它的形式逆也不含 $\alpha,\beta$。因此，提取 $\alpha\beta$ 系数时可以把这个分母直接提出，逐步得到所需恒等式。

$$
\begin{aligned}
\sum_{d\ge0}f(d,t,\{A,B\})x^d
&=[\alpha\beta]Q^{-1}(s,t)\\
&=[\alpha\beta]
\frac{[y]\operatorname{det}(\widehat Q)}
{D(\operatorname{V}(G))}\\
&=
\frac{[\alpha\beta][y]\operatorname{det}(\widehat Q)}
{D(\operatorname{V}(G))}\\
&=
\frac{
\displaystyle\sum_{P\in\mathcal P}
x^{|P|}
\left(\prod_{e\in P}z(e)\right)
D\bigl(\operatorname{V}(G)\setminus\operatorname{V}(P)\bigr)
}{
D(\operatorname{V}(G))
}
\end{aligned}
$$

### 最低次数为什么就是最优长度

对每个 $U$，$D(U)$ 都是常数项为 $1$、其他项次数为正的多项式。分母的形式逆同样只含非负次数，且常数项为 $1$。

因此，一条长度为 $|P|$ 的路径对右侧的贡献，不可能出现在低于 $|P|$ 的次数。既然所有目标简单路径的长度都至少为 $d^\ast$，所有 $d<d^\ast$ 的系数都为零。

在恰好 $d^\ast$ 次处，只能选择长度为 $d^\ast$ 的路径，再同时取路径外匹配多项式与分母逆的常数项。它们都为 $1$，所以得到以下结果。

$$
f(d^\ast,t,\{A,B\})
=
\sum_{P\in\mathcal P^\ast}\prod_{e\in P}z(e)
$$

若根本没有目标简单路径，则 $\mathcal P=\varnothing$，分子恒为零，所有长度的系数也都为零。

至此，第二种方法独立得到了与对合证明相同的结论。它也说明了为什么较大长度处不必只剩简单路径：路径外的匹配以及分母的形式逆仍可能对更高次项产生贡献。

### 随机代入与有限域运算

不同简单 $s$ 到 $t$ 路径的边集不同，因为一个简单路径边集已经唯一确定了从 $s$ 到 $t$ 的行走顺序。因此，在最短长度处，各条路径对应不同单项式，不会彼此抵消。

这个非零多项式的总次数为 $d^\ast\le V-1$。给每条边独立均匀随机取 $\mathbb F_{2^{64}}$ 中的值，根据多项式随机判零的次数界，最短长度处碰巧算成零的概率至多如下。

$$
\frac{d^\ast}{2^{64}}\le\frac{V-1}{2^{64}}
$$

比最优长度更短的结果恒为零，不存在误报更小答案的可能。无解时也一定输出 $-1$。唯一的错误方向是最短长度处随机抵消，从而输出偏大的答案或 $-1$。

每条无向边的两个方向必须使用同一个随机权值，且这个权值在所有长度层之间保持不变，否则前面的多项式恒等式不再适用。

有限域元素用次数小于 $64$、系数属于 $\mathbb F_2$ 的多项式表示。加法就是逐位异或，乘法是无进位多项式乘法，再对以下不可约多项式取模。

$$
z^{64}+z^4+z^3+z+1
$$

约简时使用以下关系。

$$
z^{64}=z^4+z^3+z+1
$$

先用无进位乘法得到至多 $128$ 位的乘积，再将高位折回低位，即可得到域内乘积。这里不能替换成普通整数乘法后截断到 $64$ 位，那是另一种运算，不能使用上述有限域上的判零结论。

每一层只依赖上一层，所以只保留相邻两层的四状态权值。层数至多为 $V-1$，每层枚举全部边，时间复杂度为 $O(V(V+E))$，空间复杂度为 $O(V+E)$，其中把固定 $64$ 位的域运算视为常数。

初始对偶图有 $O(N^2)$ 个点和边，缩点后也不会增加。按端点对汇总重边需要 $O(N^2\log N)$ 时间，包含在最终 $O(N^4)$ 的时间界内；总空间复杂度为 $O(N^2)$。这与题目对所有测试数据的 $N^4$ 之和的限制相匹配。

